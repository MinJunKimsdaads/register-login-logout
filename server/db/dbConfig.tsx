const env = require("dotenv").config({ path: ".env" });
module.exports = {
    host     : '127.0.0.1',
    user     : 'user1',
    port: '3306',
    password : 'alswns123!',
    database : 'justright',
}