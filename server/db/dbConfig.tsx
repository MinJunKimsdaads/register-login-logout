const env = require("dotenv").config({ path: ".env" });
module.exports = {
    host     : '127.0.0.1',
    user     : 'root',
    password : 'alswns123!A',
    database : 'justright',
    port: '3306',
}